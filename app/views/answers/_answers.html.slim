- @question.answers.each do |answer|
  p = answer.body
  - if user_signed_in? && current_user.author_of(answer)
    p= link_to 'Удалить', answer_path(answer.id), method: :delete, data: { confirm: 'Вы уверены?' }, class: 'btn btn-default'